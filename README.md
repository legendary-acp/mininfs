# MiniNFS – A Toy Networked File Server

## 🧩 Concept:
Implement a simplified NFS-style file server that supports basic operations over TCP from a remote client:
```
    LIST → list directory contents
    READ <filename> → stream contents
    WRITE <filename> → upload and save a file
```
